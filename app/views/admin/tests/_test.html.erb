<tr>
  <td><%= link_to test.title, admin_test_path(test) %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t("links.change"), edit_admin_test_path(test) %></td>
  <td><%= link_to t("links.delete"), admin_test_path(test),
                             method: :delete,
                             data:   { confirm: t("misc.delete_confirm") } %>
  </td>
</tr>
