<!DOCTYPE html>
<html>
  <head>
    <title>TestGuru</title>
    <meta charset="utf-8">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h1><%= link_to "TestGuru", root_path %></h1>

    <nav>
      <% if user_signed_in? %>
        <span>Привет, <%= current_user.name %> |
              <%= link_to "выйти", destroy_user_session_path, method: :delete %></span>
      <% else %>
        <span>Привет, гость: <%= link_to "вход", new_user_session_path %> |
              <%= link_to "регистрация", new_user_registration_path %> |
              <%= link_to "о TestGuru", "/about" %>
        </span>
      <% end %>
    </nav>

    <%= render "shared/flash" %>

    <%= yield %>

    <%= render "shared/footer" %>
  </body>
</html>
